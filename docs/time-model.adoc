= Deterministic guest time model

SimFerret pins the guest's QEMU time model so that the passage of time inside the
guest is a reproducible function of the fixed VM configuration and the work the
guest performs. This page is the entry point for that investigation: it maps each
artifact to what it records and gives the command that reproduces the
measurements. Current progress lives in the implementation checklist, not here.

== Where everything lives

[cols="1,2,3",options="header"]
|===
| Artifact | Path | What it records

| RFD
| link:../rfd/0004/README.adoc[RFD 4: Deterministic guest time model]
| The proposal, the determinism, compatibility, and controllability needs it separates, what it excludes, and its acceptance criteria

| Implementation checklist
| link:../rfd/0004/IMPLEMENTATION.org[RFD 0004 implementation checklist]
| Phase 0 (pin and validate the model), Phase 1 (identity, rejection, and migration), Phase 2 (contract and documentation), and the exit criteria

| Pre-RFD measurement
| link:../poc/time-model/EVIDENCE.adoc[Guest time-model measurements]
| The exploratory probe results under `shift=auto`, `shift=auto,sleep=off`, `shift=7`, `shift=7,sleep=off`, and `shift=4,sleep=off`, with their caveats and the exploratory `shift=10` observation

| Implementation evidence
| link:../rfd/0004/EVIDENCE.adoc[RFD 4 evidence]
| The pinned-scale selection and its reason, the Phase 0 validation on the reference host, the record-mode limitation that blocks pinning the model, and the re-validation trigger

| Validation gate
| link:../scripts/time-model-gate.sh[`scripts/time-model-gate.sh`]
| One run of the real probe under the pinned model with a fixed 180 s deadline and at least five runs, failing unless every requested run completed, the verdict is `reproducible`, the QEMU, kernel, and initramfs digests match the reference host's, and the guest measurements equal its recorded values; it classifies a bad record as a configuration error, an incomplete host as an unsupported execution, and a disagreement with the reference as a validation failure, and each invocation writes its own result directory

| Reference values
| link:../poc/time-model/reference-host-values.txt[`poc/time-model/reference-host-values.txt`]
| The model, the QEMU, kernel, and initramfs digests, and one measurement per metric that a host must reproduce, measured on the reference host

| Gate regression tests
| link:../scripts/time-model-gate-test.sh[`scripts/time-model-gate-test.sh`]
| The gate contract with a faked probe: a varying verdict, an incomplete sample, a differing measurement, a different artifact, and a malformed or duplicated record all fail with their own classification, a host that cannot complete is reported as unsupported without a fallback model, and a failed rerun cannot leave an earlier success standing. It also covers the host-input assertion mode below

| Host-input probe
| link:../poc/time-model/input-probe.c[`poc/time-model/input-probe.c`] with link:../scripts/time-model-input-probe.sh[`scripts/time-model-input-probe.sh`]
| Whether a guest that waits for host input receives it under a given model, reported as `delivered`, `not-delivered`, or `inconclusive` from a guest that polls until the probe stops it, with the write ordered by acknowledged stop and resume commands on QEMU's monitor, and with a non-delivery claim requiring the emulator's output to have ended in one piece, the measurement that shows the pinned model does not deliver while record/replay is enabled, and the `--expect` mode the continuous-integration job uses to assert that known result

| Host-input guest tests
| link:../poc/time-model/input-probe-test.c[`poc/time-model/input-probe-test.c`] with link:../scripts/time-model-input-probe-test.sh[`scripts/time-model-input-probe-test.sh`]
| The guest loop driven by stubs: a line that arrives after more than the polls the guest used to stop after is still reported as received, and the guest never reports giving up, so restoring either fails rather than passing

| QEMU patch spike
| link:../poc/time-model/qemu-nosleep-replay-flush.patch[`qemu-nosleep-replay-flush.patch`] with link:../poc/time-model/qemu-nosleep-replay-spike.nix[`qemu-nosleep-replay-spike.nix`]
| The minimal QEMU change that flushes queued replay events with `sleep=off`, and the scratch expression that builds the pinned QEMU with it; neither is wired into the repository's build, and the measurements are in the evidence document

| Probe source
| link:../poc/time-model/probe.c[`poc/time-model/probe.c`]
| The five guest measurements — sleep, spin, race, sleep_loop, and the checksum control — and what each one does and does not establish

| Probe harness
| link:../scripts/time-model-probe.sh[`scripts/time-model-probe.sh`]
| One pinned QEMU configuration, five requested runs, aggregation over completed runs only, and the `reproducible`, `varying`, or `inconclusive` verdict

| Harness regression tests
| link:../scripts/time-model-probe-test.sh[`scripts/time-model-probe-test.sh`]
| The harness contract with a faked QEMU: incomplete runs are excluded rather than aborting the experiment, an inconclusive sample is not reported as reproducible, and an invalid experiment fails before or without QEMU
|===

== Reproducing a measurement

[source,shell]
----
SIMFERRET_ICOUNT_OPTIONS=shift=N,sleep=off \
SIMFERRET_TIME_MODEL_OUTPUT=<directory> \
.agents/dev ./scripts/time-model-probe.sh
----

The harness writes `evidence.txt` with the configuration, the per-run status,
duration, and replay-log size, and prints the measurements it compared. Raw
per-run serial output and replay logs stay in the output directory, which defaults
to the untracked `.poc/time-model-probe` tree. `SIMFERRET_TIME_MODEL_RUNS` and
`SIMFERRET_QEMU_TIMEOUT` adjust the run count and the per-run deadline.

Phase 0 acceptance is the gate rather than the harness, because the harness
reports variation instead of failing on it:

[source,shell]
----
.agents/dev ./scripts/time-model-gate.sh
----

== What the investigation established

* Under the default `shift=auto`, the same fixed computation measured 42.65 ms and
  126.05 ms of virtual time in two runs while returning the same result, a
  deadline decision flipped between runs in the unsupported
  `shift=auto,sleep=off` combination, and the replay log differed in every run.
* Pinning the scale and disabling host-paced idle advancement made every measured
  value exact across five complete runs at `shift=2`, `shift=3`, `shift=4`,
  `shift=5`, and `shift=7`; the pinned value is recorded with its reason in the
  evidence above.
* The pinned scale is a semantic choice about an artificial performance model, not
  a correctness choice: the validated window is flat in reproducibility, and
  timeout-sensitive results are conclusions about the chosen model rather than
  predictions of production timing.
* Time, entropy, and ordering fault injection remain unimplemented, and the
  arrival timing of live host input has no deterministic position in guest virtual
  time; both are separate work.
* The pinned model cannot be adopted for record mode yet: with `sleep=off` and
  `rr=record`, QEMU queues live serial input as a replay asynchronous event and
  never flushes the queue, so a guest that waits for the control commands a
  recording is driven with never receives them. The measurement, the models it
  was compared against, and the source-level cause are in
  link:../rfd/0004/EVIDENCE.adoc[RFD 4 evidence], and
  `scripts/time-model-input-probe.sh` reproduces them. Continuous integration
  asserts that known result, so a QEMU change that fixes it fails the job
  instead of going unnoticed.
* The limitation is one early return in QEMU, and the spike recorded in
  link:../rfd/0004/EVIDENCE.adoc[RFD 4 evidence] shows that moving the flush
  ahead of it makes the pinned model record and replay: the product's record and
  two replays then match, with the same normalized events, assertions, and
  semantic outcome digest as the model in use. Adopting that patch is a project
  decision, not an open technical question.

== Related increments

* link:../rfd/0001/README.adoc[RFD 1: Deterministic execution proof of concept] established bounded record and replay.
* link:../rfd/0002/README.adoc[RFD 2: Deterministic network fault model] added the deterministic network boundary.
* link:../rfd/0003/README.adoc[RFD 3: Reproducible workload packaging] packages the workload that exercises the model.
