= SimFerret Requests for Discussion

Requests for Discussion (RFDs) capture ideas early enough to shape them through
written discussion and preserve the reasoning behind decisions. An RFD is not
authoritative merely because it exists; its `state` says how it should be read.

[cols="1,3", options="header"]
|===
| RFD | Topic

| link:0001/README.adoc[1: Deterministic execution proof of concept]
| Rust/QEMU execution architecture and a bounded record/replay demonstration with a controlled process failure

| link:0002/README.adoc[2: Deterministic network fault model]
| Record and replay a bounded network outage through an explicit QEMU network boundary

| link:0003/README.adoc[3: Reproducible workload packaging]
| Normalize standalone binaries and local OCI images into one deterministic guest workload

| link:0004/README.adoc[4: Deterministic guest time model]
| Remove host-dependent clock scaling and idle pacing from guest execution

| link:0005/README.adoc[5: CI execution and historical results MVP]
| GitHub Actions first, optional historical results, Ryusen acceptance, and a later Lighthouse review
|===

== Source format

Each RFD lives at `rfd/NNNN/README.adoc`, where `NNNN` is a four-digit number.
The document starts with canonical AsciiDoc attributes and an unpadded title:

[source,asciidoc]
----
:authors: Name <email@example.com>
:state: prediscussion
:discussion:
:labels: software, process

= RFD 7 Example title
----

`authors` contains semicolon-separated owners. `discussion` contains the RFD's
pull-request URL once discussion starts. `labels` is a comma-separated set of
searchable topics. The document is the single source of truth for this metadata;
the index intentionally does not duplicate it.

Implementation progress lives separately in `rfd/NNNN/IMPLEMENTATION.adoc`; it
is the only implementation checklist format. Its first line is
`= RFD NNNN implementation checklist`, and it contains the standalone sentence
`Implements link:README.adoc[...].`. The RFD links with
`link:IMPLEMENTATION.adoc[...]`. The RFD and checklist thus link to each other,
keeping the design and decision record stable while implementation tasks are
checked off. Each phase records its status in a `Status: DONE.` or
`Status: TODO.` line, which documents the phase and does not set the RFD's
state. The index at `rfd/README.adoc` links to every RFD with
`link:NNNN/README.adoc[...]`.

Checklist tasks are AsciiDoc checklist items: `*` bullets (`**` for nested
tasks) followed by `[ ]`, `[x]`, or the alternate checked `[*]`, plus the
project's partial `[-]` marker, which AsciiDoc renders as text. Only `[x]` and
`[*]` count as complete; an uppercase `[X]`, which AsciiDoc does not recognize
as a checkbox, is rejected. Examples that resemble tasks belong in listing
(`----`), literal (`....`), or comment (`////`) blocks, in `//` line comments,
or in inline code spans so they are excluded from status totals.

== States

* `prediscussion`: actively being written and not ready for broad review.
* `ideation`: a narrowly scoped topic or scratchpad without active revision.
* `discussion`: under active review in the linked pull request.
* `published`: discussion has converged and the RFD expresses project direction.
* `committed`: the proposal is fully implemented and describes current behavior.
* `abandoned`: deliberately not proceeding or otherwise retained only for history.

The usual path is `prediscussion` or `ideation` to `discussion`, then
`published`, and eventually `committed`. `abandoned` is an off-ramp at any stage.
Implementation checklist progress does not determine the RFD's state.

== Lifecycle

Reserve the next unused four-digit number and create `rfd/NNNN/README.adoc` and
its `IMPLEMENTATION.adoc` checklist on a branch. Cross-link the two documents.
Use `prediscussion` while writing or `ideation` for a topic placeholder. When
the document is ready for review, open a pull request, set the state to
`discussion`, and add that pull request as the discussion URL.

Before merging a proposal that represents project direction, move it to
`published`. Once the described work is entirely implemented, update it to
`committed`. Material changes to a published or committed RFD go through a new
pull request and retain the original discussion link unless the RFD explicitly
documents a replacement.
